# RanchBrain API Spec

Initial CLI/API concepts:

- status()
- ingest(module)
- search(query)
- remember(memory)
- list_modules()
- build_index()

Future HTTP endpoints:

- GET /api/ranchbrain/status
- GET /api/ranchbrain/search?q=
- POST /api/ranchbrain/remember
