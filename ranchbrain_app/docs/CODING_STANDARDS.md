# RanchBrain Coding Standards

- Python package code lives in ranchbrain_app/ranchbrain.
- Data lives in /ranchbrain.
- Keep modules small and testable.
- Prefer Python over shell for application logic.
- Use clear names.
- Add tests for every core feature.
- Do not mix OpenClaw service code with RanchBrain memory code.
