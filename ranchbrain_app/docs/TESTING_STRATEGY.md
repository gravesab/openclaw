# RanchBrain Testing Strategy

Initial tests:

- CLI status works.
- Search returns expected matches.
- Memory model validates required fields.
- Index builder creates JSON index.
- Budget importer reads CSV safely.

Tests should run locally without cloud services.
